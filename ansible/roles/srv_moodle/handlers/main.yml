---
- name: restart mariadb
  ansible.builtin.systemd:
    name: mariadb
    state: restarted
    enabled: true

- name: restart apache2
  ansible.builtin.systemd:
    name: apache2
    state: restarted
    enabled: true
