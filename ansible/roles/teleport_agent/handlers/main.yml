---
- name: restart teleport
  ansible.builtin.systemd:
    name: teleport
    state: restarted
    enabled: true
    daemon_reload: true

- name: restart sshd
  ansible.builtin.systemd:
    name: ssh
    state: restarted
